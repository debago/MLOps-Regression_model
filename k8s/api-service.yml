apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: mlops
spec:
  selector:
      app: api    # Must match the selector in the deployment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000
      containerPort: 8000
  type: LoadBalancer