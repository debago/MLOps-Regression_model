apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-api-secret
  namespace: mlops
type: Opaque
stringData:
  DB_USERNAME: "{{ .Values.secrets.api.DB_USERNAME }}"
  DB_PASSWORD: "{{ .Values.secrets.api.DB_PASSWORD }}"